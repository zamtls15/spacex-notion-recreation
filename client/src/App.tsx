/* Design philosophy: faithful Notion-style editorial document shell; quiet hierarchy, tactile restraint, responsive continuity. */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
